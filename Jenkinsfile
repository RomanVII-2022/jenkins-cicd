pipeline {
    agent any

    stages {
        stage ("hello") {
            
            steps {
                script {
                    echo currentBuild.result
                }
            }
        }
    }
}